# Environment variables for development
# This file is used directly in GitHub Codespaces (secrets are pre-loaded)
# For local development, copy to .env and replace with actual values

# Claude Code Telemetry (optional but recommended)
CLAUDE_CODE_ENABLE_TELEMETRY=1
OTEL_SERVICE_NAME=claude-code
OTEL_EXPORTER_OTLP_ENDPOINT=api.eu1.honeycomb.io:443
OTEL_EXPORTER_OTLP_PROTOCOL=grpc
OTEL_METRICS_EXPORTER=otlp

# Secrets - In Codespaces, these are automatically replaced by repository secrets
# For local development, set these in .env
OTEL_EXPORTER_OTLP_HEADERS=${OTEL_EXPORTER_OTLP_HEADERS:-x-honeycomb-team=YOUR_HONEYCOMB_API_KEY}

# Project-specific variables (customize based on your needs)
PROJECT_NAME=${PROJECT_NAME:-universal-template}
ENV=${ENV:-development}

# Example additional secrets that projects might need
# DATABASE_URL=${DATABASE_URL:-postgresql://localhost/dev}
# API_KEY=${API_KEY:-your-api-key-here}
# AWS_ACCESS_KEY_ID=${AWS_ACCESS_KEY_ID:-your-aws-key}
# AWS_SECRET_ACCESS_KEY=${AWS_SECRET_ACCESS_KEY:-your-aws-secret}

TAVILY_API_KEY=${TAVILY_API_KEY:-your-tavily-api-key}
